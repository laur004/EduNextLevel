<h1>Clase in care se preda @ViewBag.Subject.Name<h1>

        <hr>
        <br />

        <br />

        @foreach (var grade in ViewBag.Grades)
        {
            <h3>@grade.GradeName</h3>
            <a class="btn alert-primary" asp-action="ChaptersForSubjectAndGrade" asp-controller="Chapters" asp-route-subjectid="@ViewBag.Subject.Id" asp-route-gradeid="@grade.Id">Afisare clasa</a>
            @if(ViewBag.EsteAdmin==true){

            <a class="btn alert-primary" asp-action="Edit" asp-controller="Grades" asp-route-id="@grade.Id">Editare clase</a>
            }

            <br />

        }
        @*Comentariu in Razor!!*@